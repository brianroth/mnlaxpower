    <tr>
      <td><%= link_to team.name, team_path(team) %></td>
      <td><%= team.wins %></td>
      <td><%= team.losses %></td>
      <td><%= team.ties %></td>
      <td><%= team.rpi.round(3) %></td>
    </tr>